# 全面审计与修复归档 — 2026-06-10

两轮多智能体审计（security / bug / logic / token-optimization）+ 主控人工对抗验证，
覆盖 **AutoSkills**（project-manager MCP server + 11 skills + bootstrap 脚本）与
**CouncilFlow**（多模型编排 sidecar）。本文记录 finding → 任务 → commit 的对照。

## 方法

- **第一轮**：11 个并行审计员按区域分工，每个 medium+ 发现起一个对抗验证员（规模过大，中途叫停）。
- **第二轮**：6 个审计员覆盖第一轮未跑完的区域，验证由主控亲做（控制 agent 数量）。
- 合计产出 44+ 发现；主控逐条读源码验证，过滤误报后立项修复。
- 修复全程主控本地执行（controller sentinel 路由），每任务独立 commit + 自验。

## AutoSkills — v1.5.1（9 任务）

| 任务 | 严重度 | 发现 | commit |
|---|---|---|---|
| PM-701 | high | `close_task` supersede 把替代任务自身依赖边改写成 `C→C` 自依赖，死锁下游 | 8c6ab8f |
| PM-702 | medium | `create_tasks`/`add_subtask` 不查重复/空白 id，Map vs find 查找行为分裂 | 368e3ba |
| PM-703 | medium | 状态文件损坏裸抛 SyntaxError、lint 自死；多文件写半应用 + 非原子写 | 3c7a2c6 |
| PM-704 | medium | Windows 路径大小写未归一，active 项目被误判 foreign | 4c5c0c1 |
| PM-705 | low×6 | all_done 误报 / active dry_run 忽略 / cancelled+replacement 静默 / waiting_on 漏改 / archive 失败吞 / 文案 | d7cac78 |
| PM-706 | medium | bootstrap 退出码/BOM/清空 settings 三连；sync 误删；restore 不可逆 | 4947b57 |
| PM-707 | medium | skills↔CLI 契约 9 处不符（error_kind、枚举、entities、stage_gate 等） | 3806dc8 |
| PM-708 | token | skills+模板去重 −21.5KB/26%（+ follow-up 内联 discuss 恢复命令） | 79f1ad4, c5c6a08 |
| PM-709 | — | v1.5.1 发版收口（本批） | （本提交） |

## CouncilFlow — v0.2.1（10 任务）

| 任务 | 严重度 | 发现 | commit |
|---|---|---|---|
| TASK-115 | high×2 | claude prompt 经 `cmd /c` argv：命令注入面 + 8191 字符上限 → 改 stdin | 6d28445 |
| TASK-116 | medium | `discuss_wait` 写序竞态假报 summary_missing → 写序对调 + grace 重试 | 45c45fd |
| TASK-117 | medium | `claude-` 变体配置通过但 registry 无分支 → adapter_missing | 66a1a4c |
| TASK-118 | medium | 委派失败路径不恢复受保护文件；setup 异常不持久化 | 47167b8 |
| TASK-119 | medium×5 | probe 编码 / 进程树终止 / 流式末行解析 / Attempt 过宽 / mcp_policy 格式 | 60ec672 |
| TASK-120 | medium | 收敛只看末发言者，2+ 模型提前收敛；delegate 重试预写 failed | 0219874 |
| TASK-121 | medium | rename overlay 残留旧文件 / write_text 非原子 / GBK stdout 破 JSON | 71e90e7 |
| TASK-122 | medium | schema 默认值与模板漂移 100× / 引号路径 / 语言校验 / default_models 校验 | f5aa003 |
| TASK-123 | token | 讨论 prompt 压缩（早轮结构化摘要），实测无降质 | 62e1086 |
| TASK-124 | — | v0.2.1 发版收口 | （CouncilFlow 提交） |

## 验证

- AutoSkills：tsc 干净 + 26 文件 / 174 测试全过；部署 `~/.claude/mcp-project-manager`（备份 `dist.bak-v150`，已验证 version 1.5.1 + state_file_corrupt/close_task 守护就绪）。
- CouncilFlow：ruff 干净 + 全量 pytest 全过。
- TASK-123 prompt 压缩：真实 claude+codex A/B 实证未观察到质量下降（`CouncilFlow/docs/token-report-2026-06-10.md`）。

## 干净面（审计确认无问题）

状态机前向转移无非法边；`when_eval.py` AST 白名单严谨；config `extra="forbid"` typo 必报；
`.sh` 脚本引号处理全对（含含空格路径）；CouncilFlow 递归 sidecar 防护有效；
guardrails 空 writable_globs 默认拒绝。
